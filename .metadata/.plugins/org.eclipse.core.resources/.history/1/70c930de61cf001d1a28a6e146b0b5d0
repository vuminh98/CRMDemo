package demo.lambda1;

import static org.junit.Assert.*;

import org.junit.Test;

import junit.framework.Assert;

public class WelcomeLambdaTest2 {

	@Test
	public void testHandleRequest() {
		int num = 3;
		final int exped = 2;
		final int actual = WelcomeLambda.findFibonacciPosition(num);
		assertEquals(exped, actual);
	}

}
